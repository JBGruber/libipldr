# libipldr 0.1.1

* CRAN resubmission addressing reviewer feedback:
  - Software names ('Rust', 'IPLD', 'IPFS', 'AtProto') now quoted in Title and Description.
  - Acronyms DAG-CBOR, IPLD, and IPFS expanded in Description.
  - Added upstream library reference to Description.
  - Replaced `\dontrun{}` with `\donttest{}` in `decode_car()` example.
  - Added `inst/extdata/sample.car` for use in the `decode_car()` example.

# libipldr 0.1.0

* Initial CRAN submission.
